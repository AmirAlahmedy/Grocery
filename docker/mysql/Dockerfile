# Use the official MySQL image from Docker Hub
FROM mysql:latest

# Set the MySQL root password environment variable
ENV MYSQL_ROOT_PASSWORD=your_secure_password_here

# Optional: You can add additional configurations if needed
# For example, creating a specific database or user
# ENV MYSQL_DATABASE=myapp
# ENV MYSQL_USER=myuser
# ENV MYSQL_PASSWORD=mypassword

# Expose the default MySQL port
EXPOSE 3306